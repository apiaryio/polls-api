machine:
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test

dependencies:
  cache_directories:
    - cache
  post:
    - which cache/node_modules/dredd/bin/dredd || install dredd --prefix cache

test:
  pre:
    - python manage.py migrate
  post:
    - ./scripts/dredd
