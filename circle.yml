machine:
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test

dependencies:
  post:
    - npm install -g dredd

test:
  pre:
    - python manage.py migrate
  post:
    - ./scripts/dredd
